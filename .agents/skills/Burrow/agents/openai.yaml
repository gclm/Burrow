interface:
  display_name: "Burrow"
  short_description: "Repo-specific patterns and workflows for Burrow"
  default_prompt: "Use the Burrow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true